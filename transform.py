import re
import sys

sys.stdout.reconfigure(encoding='utf-8')

with open('index.html', 'r', encoding='utf-8') as f:
    content = f.read()

# 각 intro-text의 OLD → NEW 교체 목록
# 형식: 화면소개 1줄 + <br>\n    + 기능소개1. 기능소개2. (no <br> in between)

replacements = [

    # ── QC 결과 확인 ──────────────────────────────────────────────────────
    (
        """    QC 결과 확인은 <strong>특정 장비·물질의 상세 결과를 Levey-Jennings 차트와 함께 확인</strong>하는 화면이에요.<br>
    이상 결과가 발생했을 때 원인을 추적하거나 후속 처리를 할 때 사용해요.
    테이블에서 결과를 선택하면 LJ 차트에 위치가 연동되고, 이상치에 Comment를 등록하거나 결과를 삭제할 수 있어요. 그리고, 조회 결과는 저장 맟 인쇄할 수 있어요.""",
        """    QC 결과 확인은 <strong>특정 장비·물질의 상세 결과를 Levey-Jennings 차트와 함께 확인</strong>하는 화면이에요.<br>
    테이블에서 결과를 선택하면 LJ 차트에 위치가 연동되고, 이상치에 Comment 등록이나 결과 삭제도 할 수 있어요. 조회 결과는 Excel로 저장하거나 차트·테이블을 함께 인쇄할 수 있어요."""
    ),

    # ── 대시보드 ───────────────────────────────────────────────────────────
    (
        """    정도관리 결과를 <strong>상단 요약 지표 + 레벨별 현황 + 상세 테이블 + LJ 차트</strong>로 한 번에 확인하는 메인 화면이에요.<br>
    어떤 레벨에서 거부가 발생했는지, 최근 추세가 흔들리는지, 통계값이 안정적인지 빠르게 판단할 때 사용해요.
    도넛 차트나 LJ 차트 범례로 특정 LOT·레벨만 선택해 집중적으로 보거나, Multi Sort로 결과를 정렬해 이상 데이터를 빠르게 추적할 수 있어요.""",
        """    정도관리 결과를 <strong>상단 요약 지표 + 레벨별 현황 + 상세 테이블 + LJ 차트</strong>로 한 번에 확인하는 메인 화면이에요.<br>
    레벨별·검사별 보기를 전환하거나 일별·주별·월별 단위로 기간을 바꿔 단기 이상과 장기 추세를 함께 점검할 수 있어요. 도넛 차트나 LJ 차트 범례로 특정 LOT·레벨만 선택해 집중적으로 보거나, Multi Sort로 이상 데이터를 빠르게 추적할 수 있어요."""
    ),

    # ── 바코드 출력 ────────────────────────────────────────────────────────
    (
        """    QC 물질을 선택해 접수용 바코드를 생성하고 출력하는 화면이에요.<br>
    접수 버튼으로 바코드를 생성하고 바코드를 인쇄할 수 있어요. 라벨 위치·크기 등 출력 설정을 저장해두면 다음 출력에도 그대로 적용할 수 있어요.""",
        """    QC 물질을 선택해 접수용 바코드를 생성하고 출력하는 화면이에요.<br>
    접수와 동시에 인쇄하거나, 하단 이력 목록에서 재출력·접수 취소를 진행할 수 있어요. 라벨 위치·크기 등 출력 설정을 저장해두면 다음 출력에도 그대로 적용할 수 있어요."""
    ),

    # ── QC 데이터 조회 ─────────────────────────────────────────────────────
    (
        """    대시보드보다 세밀한 조건으로 QC 데이터를 조회할 때 사용해요.<br>
    상태·규칙·Comment 등록 여부까지 필터링할 수 있고, Comment 등록·Excel 출력·Rule Refresh 등 후속 작업도 이 화면에서 진행해요.<br>
    중복 결과를 자동으로 체크해 취소 처리하거나, Multi Sort로 여러 컬럼을 동시에 정렬해 원하는 데이터를 빠르게 찾을 수 있어요.""",
        """    대시보드보다 세밀한 조건으로 QC 데이터를 조회하고 후속 처리까지 진행하는 화면이에요.<br>
    상태·규칙·Comment 여부까지 필터링해 조회하고, Comment 등록·Excel 출력·Rule Refresh도 이 화면에서 진행해요. 중복 결과를 자동 체크해 취소 처리하거나, Multi Sort로 여러 컬럼을 동시에 정렬해 원하는 데이터를 빠르게 찾을 수도 있어요."""
    ),

    # ── 환자 결과 확인 ─────────────────────────────────────────────────────
    (
        """    AgentQC에서 QC 데이터뿐 아니라 <strong>환자 데이터도 조회</strong>할 수 있는 화면이에요.<br>
    검사실·장비·날짜 조건 또는 검체번호로 원하는 환자 데이터를 바로 검색할 수 있어요.<br>
    선택한 환자의 상세 결과와 스캐터그램(RBC, PLT, WBC, WNB, RET, RBC-VHF)을 함께 확인하고, 다운로드·인쇄와 FTP 전송 상태도 이 화면에서 볼 수 있어요.""",
        """    AgentQC에서 QC 데이터뿐 아니라 <strong>환자 데이터도 조회</strong>할 수 있는 화면이에요.<br>
    검사실·장비·날짜 조건 또는 검체번호로 원하는 환자 데이터를 바로 검색할 수 있어요. 선택한 환자의 상세 결과와 스캐터그램(RBC, PLT, WBC, WNB, RET, RBC-VHF)을 함께 확인하고, 다운로드·인쇄와 FTP 전송 상태도 이 화면에서 볼 수 있어요."""
    ),

    # ── X-Bar 차트 ────────────────────────────────────────────────────────
    (
        """    환자 데이터를 일정 개수(기본 20개)로 묶어 평균을 내고, 그 변화 추이를 시각적으로 확인하는 차트예요.<br>
    QC 데이터 없이도 장비 안정성을 모니터링할 때 사용해요.""",
        """    환자 데이터를 일정 개수(기본 20개)로 묶어 평균을 내고, 그 변화 추이를 시각적으로 확인하는 차트예요.<br>
    상한·하한선을 벗어난 포인트는 빨간 점으로 표시되며, 포인트 위에 마우스를 올리면 상세 정보를 확인할 수 있어요. 장비 탭을 전환하며 여러 장비의 X-Bar를 비교할 수 있어요."""
    ),

    # ── X-Bar 설정 ────────────────────────────────────────────────────────
    (
        """    X-Bar 차트에 사용될 <strong>Mean / Limit 기준값을 검사항목별로 설정</strong>하는 화면이에요.<br>
    처음 X-Bar를 사용하거나 기준값을 변경할 때 이 화면에서 설정해요.<br>
    등록된 기준값은 수정·삭제할 수 있고, 지정한 기간의 실측 데이터를 기반으로 Mean을 다시 계산해 Target을 재설정할 수도 있어요.""",
        """    X-Bar 차트에 사용될 <strong>Mean / Limit 기준값을 검사항목별로 설정</strong>하는 화면이에요.<br>
    기준값을 신규 등록하거나 기존 값을 수정·삭제할 수 있어요. 지정한 기간의 실측 데이터를 기반으로 Mean을 다시 계산해 Target을 재설정할 수도 있어요."""
    ),

    # ── 장비간 상관성 평가 ──────────────────────────────────────────────────
    (
        """    같은 검사를 수행하는 여러 장비의 결과가 서로 일치하는지 비교하는 화면이에요.<br>
    기준 장비 1대와 비교 장비 최대 3대를 동시에 비교할 수 있어요.<br>
    일별·주별·월별 단위로 기간을 바꿔 분석하고, 회귀선과 통계 요약으로 상관성을 확인해요. 결과 표를 복사하거나 Excel 저장·인쇄, 계산 검증도 이 화면에서 진행해요.""",
        """    같은 검사를 수행하는 여러 장비의 결과가 서로 일치하는지 비교하는 화면이에요.<br>
    기준 장비 1대와 비교 장비 최대 3대를 일별·주별·월별 단위로 비교하고, 회귀선·통계 요약으로 상관성을 확인할 수 있어요. 결과 표를 복사하거나 Excel 저장·인쇄, 계산 검증도 이 화면에서 진행해요."""
    ),

    # ── 허용범위 보고서 ────────────────────────────────────────────────────
    (
        """    검사실 인증·평가에 필요한 정도관리 보고서를 조회하고 출력하는 화면이에요.<br>
    이 중 <strong>허용범위 보고서</strong>는 기간 내 QC 결과를 바탕으로 제조사 기준과 실측 통계를 비교해 적합 여부를 확인해요.<br>
    Reject 데이터를 제외한 상태로 통계를 재계산하거나, 계산 검증으로 수치를 별도 검토할 수 있어요. 결과는 Excel 저장 또는 인쇄로 출력해요.""",
        """    <strong>허용범위 보고서</strong>는 기간 내 QC 결과를 바탕으로 제조사 기준과 실측 통계를 비교해 적합 여부를 확인하는 화면이에요.<br>
    Reject 데이터를 제외한 상태로 통계를 재계산하거나, 계산 검증으로 수치를 별도 검토할 수 있어요. 결과는 Excel 저장 또는 인쇄로 출력해요."""
    ),

    # ── CV Report ─────────────────────────────────────────────────────────
    (
        """    기간별 QC 결과를 바탕으로 <strong>월별 CV(%) 추이와 평균 CV</strong>를 확인하는 보고서예요.<br>
    숫자 테이블과 추이 그래프를 함께 보며 장비·물질·검사 항목·레벨별 월별 변화를 비교하고 안정성을 점검해요.<br>
    Reject 데이터를 제외해 재계산할 수 있고, 결과는 Excel 저장 또는 인쇄로 출력해요.""",
        """    기간별 QC 결과를 바탕으로 <strong>월별 CV(%) 추이와 평균 CV</strong>를 확인하는 보고서예요.<br>
    숫자 테이블과 추이 그래프를 함께 보며 장비·물질·검사 항목·레벨별 월별 변화를 비교하고 안정성을 점검해요. Reject 데이터를 제외해 재계산할 수 있고, 결과는 Excel 저장 또는 인쇄로 출력해요."""
    ),

    # ── 정성 Report ───────────────────────────────────────────────────────
    (
        """    기간 내 QC 결과를 기준 Range와 비교해 <strong>Pass / N/A</strong> 여부를 확인하는 정성 보고서예요.<br>
    검사실, 장비, 물질, 검사 항목, 레벨 조건에 따라 개별 결과의 적합 여부를 빠르게 검토할 수 있어요.<br>
    조회 건수는 화면 상단에서 바로 확인하고, 결과는 Excel 저장 또는 인쇄로 출력해요.""",
        """    기간 내 QC 결과를 기준 Range와 비교해 <strong>Pass / N/A</strong> 여부를 확인하는 정성 보고서예요.<br>
    검사실, 장비, 물질, 검사 항목, 레벨 조건에 따라 개별 결과의 적합 여부를 빠르게 검토할 수 있어요. 조회 건수는 화면 상단에서 바로 확인하고, 결과는 Excel 저장 또는 인쇄로 출력해요."""
    ),

    # ── 보고서 통합 조회 ───────────────────────────────────────────────────
    (
        """    기간 조건으로 이미 생성된 보고서를 한 화면에서 다시 조회하는 메뉴예요.<br>
    검사실·장비 조건을 추가해 원하는 보고서만 빠르게 찾을 수 있고, 목록에서 보고서를 선택해 내용을 다시 확인하거나 다운로드할 수 있어요.""",
        """    기간 조건으로 이미 생성된 보고서를 한 화면에서 다시 조회하는 메뉴예요.<br>
    검사실·장비 조건을 추가해 원하는 보고서만 빠르게 찾을 수 있어요. 목록에서 보고서를 선택해 내용을 다시 확인하거나 다운로드할 수 있어요."""
    ),

    # ── 검사항목 ──────────────────────────────────────────────────────────
    (
        """    병원에서 정도관리하는 검사항목을 등록·관리하는 화면이에요.<br>
    새로운 검사항목이 추가될 때 이 화면에서 먼저 등록해야 QC LOT에서 사용할 수 있어요.<br>
    기존 항목의 수정과 삭제도 이 화면에서 진행해요.""",
        """    병원에서 정도관리하는 검사항목을 등록·관리하는 화면이에요.<br>
    새로운 검사항목이 추가될 때 이 화면에서 먼저 등록해야 QC LOT에서 사용할 수 있어요. 기존 항목의 수정과 삭제도 이 화면에서 진행해요."""
    ),

    # ── QC LOT ────────────────────────────────────────────────────────────
    (
        """    검사실·장비·검사항목을 하나의 LOT으로 묶고 타깃값(Mean, SD)을 설정하는 화면이에요.<br>
    새 물질을 사용하거나 LOT를 교체할 때, 또는 제조사 타깃값을 불러올 때 사용해요.<br>
    타깃값은 직접 입력하거나 제조사 데이터를 자동으로 불러올 수 있고, 일괄 적용으로 모든 검사항목에 동일한 값을 한 번에 반영할 수 있어요. 등록된 LOT의 값 수정과 종료일자 변경도 이 화면에서 진행해요.""",
        """    검사실·장비·검사항목을 하나의 LOT으로 묶고 타깃값(Mean, SD)을 설정하는 화면이에요.<br>
    타깃값은 직접 입력하거나 제조사 데이터를 자동으로 불러올 수 있고, 일괄 적용으로 모든 검사항목에 동일한 값을 한 번에 반영할 수 있어요. 등록된 LOT의 값 수정과 종료일자 변경도 이 화면에서 진행해요."""
    ),

    # ── Target/허용범위 ────────────────────────────────────────────────────
    (
        """    우리 병원의 실제 QC 데이터를 기반으로 <strong>"이 범위까지는 정상으로 보겠다"는 기준을 새로 잡는 작업</strong>이에요.<br>
    QC LOT를 일정 기간 사용한 뒤, 새 LOT로 교체하기 전에 이 화면에서 허용범위를 설정해요.<br>
    조건 설정 후 집계 버튼을 눌러 해당 기간의 통계를 산출하고, 별도 데이터는 우클릭·더블클릭으로 개별 값을 추가하거나 제거할 수 있어요. 최종 기준을 확인한 뒤 QC 마스터로 일괄 저장해요.""",
        """    우리 병원의 실제 QC 데이터를 기반으로 <strong>"이 범위까지는 정상으로 보겠다"는 기준을 새로 잡는 작업</strong>이에요.<br>
    조건 설정 후 집계 버튼으로 해당 기간의 통계를 산출하고, 별도 데이터는 우클릭·더블클릭으로 개별 값을 추가하거나 제거할 수 있어요. 최종 기준을 확인한 뒤 QC 마스터로 일괄 저장해요."""
    ),

    # ── Reagent LOT ───────────────────────────────────────────────────────
    (
        """    병원에서 사용하는 <strong>시약을 등록</strong>하고, 시약별 <strong>LOT를 관리</strong>하는 화면이에요.<br>
    시약 LOT는 <strong>Pooled SD</strong>를 산출하기 위한 기준 LOT로, QC LOT와 달리 타깃값 없이 LOT NO만 입력하면 돼요.<br>
    <strong>* Pooled SD</strong>란 같은 검사 조건에서 나온 QC 결과들을 모아 계산한 평균적인 SD예요.<br>
    해당 시약·장비에서 QC 값이 얼마나 안정적으로 나오는지 보여주는 값이에요.<br>
    시약을 처음 등록할 때는 시약 등록 버튼으로 시약코드·시약명을 신규 등록하고, 이후 LOT NO를 입력해 저장해요. 등록된 내역은 하단 리스트에서 수정·삭제하거나 Excel로 저장할 수 있어요.""",
        """    병원에서 사용하는 <strong>시약을 등록</strong>하고, Pooled SD 산출을 위한 시약별 <strong>LOT를 관리</strong>하는 화면이에요.<br>
    시약코드·시약명을 먼저 등록한 뒤 검사항목별로 LOT NO를 입력해 저장해요. 등록된 내역은 하단 리스트에서 수정·삭제하거나 Excel로 저장할 수 있어요."""
    ),

    # ── 기본 마스터 관리 ───────────────────────────────────────────────────
    (
        """    검사실·장비·MultiRule·Comment·단위 등 정도관리 기본 환경을 설정하는 화면이에요.<br>
    사용자 설정은 별도 섹션에서 설명하고, 여기서는 기본 마스터에서 공통으로 다루는 핵심 항목을 먼저 정리해요.""",
        """    검사실·장비·MultiRule·Comment·단위 등 정도관리 기본 환경을 설정하는 화면이에요.<br>
    마스터 구분 드롭다운으로 설정 항목을 전환하고, 신규·수정·삭제 버튼으로 마스터 데이터를 관리할 수 있어요. 사용자 계정 설정은 별도 섹션(계정 생성 / 비밀번호 변경)에서 확인할 수 있어요."""
    ),

    # ── 검사실별 물질 관리 ─────────────────────────────────────────────────
    (
        """    병원에서 사용하는 QC 물질을 등록·관리하는 화면이에요.<br>
    새로운 물질이 들어왔을 때 여기서 먼저 등록해야 QC LOT에서 선택할 수 있어요.<br>
    기존 물질의 수정과 삭제도 이 화면에서 진행해요.""",
        """    병원에서 사용하는 QC 물질을 등록·관리하는 화면이에요.<br>
    새로운 물질이 들어왔을 때 이 화면에서 먼저 등록해야 QC LOT에서 선택할 수 있어요. 기존 물질의 수정과 삭제도 이 화면에서 진행해요."""
    ),
]

for old, new in replacements:
    if old in content:
        content = content.replace(old, new, 1)
        label = old.strip()[:50]
        print(f"OK: {label}")
    else:
        label = old.strip()[:60]
        print(f"NOT FOUND: {label}")

with open('index.html', 'w', encoding='utf-8') as f:
    f.write(content)

print(f"\nDone. Lines: {content.count(chr(10))}")
